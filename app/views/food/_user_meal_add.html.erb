<div class="tab-content">
  <div class="tab-pane active" id="recent">
       <form class="add-food" action="food/add_item" method="post"> 
   <% recent.each do |item| %>
     <div class="radio">
       <label>
       <input type="radio" name="food_item" value=<%= item.food.id %> >
       <%= item.food.name %> </br>
       </label>
             <input type="hidden" name="d" value=<%=params[:d]%> />
             <input type="hidden" name="meal" value=<%=params[:meal]%> />
     </div>
   <% end %>
         <button type="submit" class="btn btn-default">Save</button>
   </form> 
  </div>

  <div class="tab-pane" id="frequent">
       <form class="add-food" action="food/add_item" method="post"> 
   <% frequent.each do |item| %>
     <div class="radio">
       <label>
       <input type="radio" name="food_item" value=<%= item.food.id %> >
       <%= item.food.name %> </br>
       </label>
             <input type="hidden" name="d" value=<%=params[:d]%> />
             <input type="hidden" name="meal" value=<%=params[:meal]%>/>
     </div>
   <% end %>
         <button type="submit" class="btn btn-default">Save</button>
   </form> 
  </div>

  <div class="tab-pane" id="allmeals">
       <form class="add-food" action="food/add_item" method="post"> 
        <% allmeal.each do |item| %>
          <div class="radio">
             <label>
             <input type="radio" name="food_item" value=<%= item.food.id %> />
               <%= item.food.name %> </br>
             </label>
             <input type="hidden" name="d" value=<%=params[:d]%> />
             <input type="hidden" name="meal" value=<%=params[:meal]%> />
          </div>
        <% end %>
         <button type="submit" class="btn btn-default">Save</button>
      </form>
  </div>
</div> <!-- end .tab-content -->

<!--
<div class="modal fade" id="post_meal" tabindex="-1" role-"dialog" aria-labelledby="mealModal" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="myModalLabel"
-->
